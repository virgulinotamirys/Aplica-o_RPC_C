struct valores{
	float a;
	float b;
};

program calculadora{
	version calc{
		float soma(valores) = 1;
		float multiplicacao(valores) = 2;
		float subtracao(valores) = 3;
		float divisao(valores) = 4;
	} = 5;
} = 6;
